# VarDumpServer

## Usage
```
composer install
```
Set vendor path:
```
PATH="./vendor/bin:$PATH"
```

Start server:
```
var-dump-server
```

Start server and output to external file:
```
var-dump-server --format="html" > dump.html
```